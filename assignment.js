const add =(a,b)=>{
    return a+b;
}

const subtract =(a,b)=>{
    return a-b;
}

const divide =(a,b)=>{
    return a/b;
}

const multiply =(a,b)=>{
    return a*b;
}

let aimr=6;
let sumis=8;
console.log(add(aimr,sumis));
console.log(subtract(aimr,sumis));
console.log(divide(aimr,sumis));
console.log(multiply(aimr,sumis));
